#ifndef PIXELSIM_UTILS_H
#define PIXELSIM_UTILS_H

#define MAX(a, b) ((a) > (b) ? (a) : (b))

double closen_to_zero(double x, double y);

void normalize(double* x, double* y);

#endif //PIXELSIM_UTILS_H